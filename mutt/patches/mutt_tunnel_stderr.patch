diff -r 79ea10b2d81c mutt_tunnel.c
--- a/mutt_tunnel.c	Sat Jan 03 11:33:10 2015 -0800
+++ b/mutt_tunnel.c	Tue Jan 06 03:47:17 2015 +0200
@@ -85,14 +85,17 @@
   mutt_block_signals_system ();
   if ((pid = fork ()) == 0)
   {
+    int devnull;
     mutt_unblock_signals_system (0);
-    if (dup2 (pout[0], STDIN_FILENO) < 0 || dup2 (pin[1], STDOUT_FILENO) < 0)
+    devnull = open ("/dev/null", O_WRONLY);
+    if (devnull < 0 || dup2 (pout[0], STDIN_FILENO) < 0 || dup2 (pin[1],
+	STDOUT_FILENO) < 0 || dup2 (devnull, STDERR_FILENO) < 0)
       _exit (127);
     close (pin[0]);
     close (pin[1]);
     close (pout[0]);
     close (pout[1]);
-    close (STDERR_FILENO);
+    close (devnull);
 
     /* Don't let the subprocess think it can use the controlling tty */
     setsid ();
